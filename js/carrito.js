/*const productosEnCarrito = JSON.parse(localStorage.getItem("carrito"));
console.log(productosEnCarrito);*/

/*function actualizarCarrito() {

    const productosEnCarrito = JSON.parse(localStorage.getItem("carrito"));
    listaCarrito.innerHTML = ""; 

    carrito.forEach((producto) => {
        const itemCarrito = document.createElement("div");
        itemCarrito.className = "card col-3 mr-5 mb-5";
        itemCarrito.innerHTML = `
        <img src="${producto.img}" class="card-img-top" alt="${procuto.nombre}">
        <div class="card-body">
           <h5 class="card-title">${producto.nombre}</h5>
           <p class="card-text">${producto.texto} <br><br> Precio:  $${producto.precio}</p>
           <button id="${producto.id}" class="btn btn-danger">Eliminar</button>
        </div> `;

        listaCarrito.appendChild(itemCarrito);

    });*/
